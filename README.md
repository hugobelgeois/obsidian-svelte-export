- Want specific page to be full width :
  add `full-width: true` to the frontmatter
- Want specific page to be full height :
  add `full-height: true` to the frontmatter
- Want to hide the title on a specific page :
  add `hide-title: true` to the frontmatter
